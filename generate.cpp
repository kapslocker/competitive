#include <bits/stdc++.h>

using namespace std;
int main() {
    fstream fout;
    fout.open("input", ios::out);
    fout << 500 << endl;
    for(int i = 0; i < 500; i++)
        fout << 1000 << " ";
    fout.close();
    return 0;
}
