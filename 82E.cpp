#include <bits/stdc++.h>

using namespace std;

#define long long int
int main() {
    int n, t;
    double p;
    cin >> n >> p >> t;
    if(n >= t) {
        cout << p * t << endl;
        return 0;
    }
    
    return 0;
}
